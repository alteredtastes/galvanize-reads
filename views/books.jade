extends layout

block content
  for book_entries in title
    table
      tr
        td #{book_entries.title}
        td #{book_entries.genre}
        td #{book_entries.description}
        td #{book_entries.url}
